type UserType {
    id: ID
    name: String
    surname: String
    username: String
    role: Int
    token: String
    # lastLoginAt: DateTime!
    # createdAt: DateTime!
    # updatedAt: DateTime!
    enabled: Boolean
}

union UserUnion = UserType | ApiError

type Query {
    # getUser(
    #     id: String!): UserType!
    getUser(
        id: String!): UserUnion!
    # getUsers: [UserUnion]!
}